services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hjem-i-kroppen-frontend
    restart: unless-stopped
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true